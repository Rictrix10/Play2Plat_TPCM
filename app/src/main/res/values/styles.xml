<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@drawable/background</item> <!-- Defina o plano de fundo da janela -->
        <item name="android:colorPrimary">@color/purple_500</item> <!-- Defina a cor primária -->
        <item name="android:colorPrimaryDark">@android:color/transparent</item> <!-- Define a cor primária escura como transparente -->
        <item name="android:colorAccent">@color/light_blue_600</item> <!-- Define a cor de destaque -->
    </style>
</resources>
